# Unreal Engine CLI - Requirements
# This file lists all runtime dependencies for the package

# Core dependencies
click>=8.1.0
pydantic>=2.0.0
colorama>=0.4.6

# REPL and UI dependencies
prompt-toolkit>=3.0.0
rich>=13.0.0
pygments>=2.15.0

# Utility dependencies
pathspec>=0.11.0
toml>=0.10.0
pyyaml>=6.0.0

# Platform-specific dependencies (optional)
# These are only needed on specific platforms
# windows-curses>=2.3.0 ; platform_system == "Windows"